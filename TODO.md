# TODO

## Agent Restart Button — Railway Config Needed
- [ ] Set `RAILWAY_LIGHT_SERVICE_ID` env var on the API service (find it in Railway dashboard → light workers service → Settings)
- [ ] Verify `RAILWAY_HEAVY_SERVICE_ID` and `RAILWAY_API_TOKEN` are already set (used by autoscaler)
- [ ] Deploy and test the restart button on /agents page
